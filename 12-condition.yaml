- name: condition
  hosts: local
  connection: local
  vars:
    number: 12
  tasks:
  - name: check given number is less then 10 or not
    ansible.builtin.debug:
      msg: " given number is {{ number }} is less then 10 or not"
    when: number < 10
  