- name: install packages 
  hosts: local
  connection: local
  become: yes
  tasks:
  - name: install packages on linux server
    ansible.builtin,debug:
      name: "{{ item }}"
      state: latest
    loop:
    - nginx
    - mysql
    - zip